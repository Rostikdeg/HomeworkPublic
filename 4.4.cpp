#include <iostream>

using namespace std;
int main()

{

	int a = 1, b = 1, c;
    
	if (c = (a - b)) // Це тому, що там тільки один = і воно надає значення. А значення дорівнює 0 тому False

    	cout << "The value of c is: " << c;
	return 0;//Також тут немає {} тому return теж не виконується

}